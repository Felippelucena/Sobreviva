{
  "name": "sobreviva",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Open-source Vampire Survivors-style browser game with in-game editor and mod support.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "pixi.js": "^8.6.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "~5.6.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.5"
  }
}
